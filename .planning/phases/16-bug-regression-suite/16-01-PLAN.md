---
phase: 16-bug-regression-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/regression/conftest.py
  - tests/e2e/regression/test_tp22.py
  - tests/e2e/regression/test_tp25.py
  - tests/e2e/regression/test_tp29.py
  - tests/e2e/regression/test_tp34.py
  - tests/e2e/.env
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - REG-04

must_haves:
  truths:
    - "Running pytest -m regression_bugs executes redirect regression tests for TP-22, TP-25, TP-29, TP-34"
    - "Each test file has a docstring referencing the Jira ticket ID and describing the original bug behavior"
    - "TP-22 test verifies non-existent key redirects to trafficportal.com instead of error/blank"
    - "All redirect tests create their own preconditions inline and do not depend on pre-existing data"
    - "Tests use httpx with follow_redirects=False to inspect raw redirect responses"
  artifacts:
    - path: "tests/e2e/regression/conftest.py"
      provides: "Regression fixtures: unique_keyword, http_client, api_client"
      contains: "unique_keyword"
    - path: "tests/e2e/regression/test_tp22.py"
      provides: "TP-22 non-existent key redirect test"
      contains: "regression_bugs"
    - path: "tests/e2e/regression/test_tp25.py"
      provides: "TP-25 device-based redirect test"
      contains: "regression_bugs"
    - path: "tests/e2e/regression/test_tp29.py"
      provides: "TP-29 domain redirect test"
      contains: "regression_bugs"
    - path: "tests/e2e/regression/test_tp34.py"
      provides: "TP-34 Set redirect test"
      contains: "regression_bugs"
  key_links:
    - from: "tests/e2e/regression/test_tp22.py"
      to: "httpx"
      via: "http_client fixture from conftest"
      pattern: "http_client"
    - from: "tests/e2e/regression/conftest.py"
      to: "tests/e2e/.env"
      via: "TP_SHORT_DOMAIN env var"
      pattern: "TP_SHORT_DOMAIN"
---

<objective>
Write regression tests for the four redirect-layer Jira bugs (TP-22, TP-25, TP-29, TP-34) and set up shared regression fixtures in conftest.py.

Purpose: Prevent silent re-introduction of redirect bugs by automating the exact failure scenarios from each Jira ticket.
Output: 4 test files + updated conftest.py with shared fixtures for all regression tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bug-regression-suite/16-RESEARCH.md
@tests/e2e/regression/conftest.py
@tests/e2e/conftest.py
@tests/e2e/.env
@tests/e2e/stress/test_generate_usage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression conftest fixtures and TP-22 test</name>
  <files>tests/e2e/regression/conftest.py, tests/e2e/regression/test_tp22.py, tests/e2e/.env</files>
  <action>
1. Update `tests/e2e/regression/conftest.py` with shared fixtures for all regression tests:
   - `unique_keyword` fixture: generates `reg-{uuid.uuid4().hex[:8]}` for test isolation
   - `http_client` fixture: httpx.Client with timeout=15.0, verify=False, follow_redirects=False (for redirect tests)
   - `api_client` fixture: httpx.Client with API_KEY header, pytest.skip if API_KEY not set
   - Import SHORT_DOMAIN from env var `TP_SHORT_DOMAIN` (default `dev.trfc.link`)
   - Import API_ENDPOINT and API_KEY from env vars
   - Keep existing docstring updated

2. Add `TP_SHORT_DOMAIN=dev.trfc.link` to `tests/e2e/.env` if not already present.

3. CRITICAL: Before writing test_tp22.py, read the actual Jira ticket TP-22 at bloomland.atlassian.net. Use `gh` CLI or ask the user for ticket details. The test docstring MUST describe the original bug behavior from the ticket, not just the requirement description.

4. Create `tests/e2e/regression/test_tp22.py` (REG-01):
   - Module docstring: Jira ticket ID, original bug description, fix description, link to ticket
   - Class `TestTP22` with `@pytest.mark.regression_bugs` decorator
   - `test_nonexistent_key_redirects_to_default(self, http_client)`: GET `https://{SHORT_DOMAIN}/this-key-does-not-exist-tp22-{uuid}` with follow_redirects=False. Assert 301/302 status. Assert Location header contains "trafficportal.com".
   - `test_empty_key_redirects_to_default(self, http_client)`: GET `https://{SHORT_DOMAIN}/` (root path). Assert redirect to trafficportal.com or appropriate default behavior.
   - Each test uses a fresh unique key so there is no caching interference.
   - No preconditions needed -- tests use keys guaranteed not to exist.

If Jira is not accessible, ask the user: "I need the details from Jira ticket TP-22 to write an accurate regression test. Can you provide the bug description, reproduction steps, and expected behavior?"
  </action>
  <verify>
Run: `cd tests/e2e && python -m pytest regression/test_tp22.py -m regression_bugs -v --timeout=30 2>&1 | head -30`
Tests should either pass (bug is fixed) or fail with clear assertion messages (bug has regressed). No import errors or fixture errors.
  </verify>
  <done>conftest.py has unique_keyword, http_client, and api_client fixtures. test_tp22.py has at least 2 test methods covering non-existent and empty key redirect scenarios, each with a descriptive docstring referencing TP-22.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect regression tests for TP-25, TP-29, TP-34</name>
  <files>tests/e2e/regression/test_tp25.py, tests/e2e/regression/test_tp29.py, tests/e2e/regression/test_tp34.py</files>
  <action>
CRITICAL: Before writing each test file, read the corresponding Jira ticket at bloomland.atlassian.net. If Jira is not accessible, ask the user for each ticket's details. The test docstrings MUST describe the original bug behavior.

1. Create `tests/e2e/regression/test_tp25.py` (REG-02 -- device-based redirect):
   - Module docstring with TP-25 bug description from Jira
   - Class `TestTP25` with `@pytest.mark.regression_bugs`
   - Define UA constants: MOBILE_UA (iPhone Safari), DESKTOP_UA (Chrome Windows), and QR detection (either `?qr=1` query param or specific UA)
   - This test likely needs an existing link with device-specific redirect rules. If the test needs a link, create one via UI using the `client_links_page` fixture with a unique keyword, then test redirects against it.
   - `test_mobile_redirect(self, http_client)`: GET link URL with mobile User-Agent. Assert correct redirect target.
   - `test_desktop_redirect(self, http_client)`: GET link URL with desktop User-Agent. Assert correct redirect target.
   - `test_qr_redirect(self, http_client)`: GET link URL with `?qr=1`. Assert correct redirect.
   - If device-specific redirect is not a feature (determined from Jira ticket), write defensive tests that verify no errors occur when accessing with different User-Agents.

2. Create `tests/e2e/regression/test_tp29.py` (REG-03 -- domain redirect):
   - Module docstring with TP-29 bug description from Jira
   - Class `TestTP29` with `@pytest.mark.regression_bugs`
   - `test_nonexistent_domain_redirect`: GET a URL on a domain that does not exist in the system. Assert graceful redirect (not 500 error).
   - `test_domain_without_key_redirect`: GET the root path on a configured domain. Assert appropriate redirect.
   - `test_subdomain_with_key_redirect`: GET a path on a subdomain. Assert correct redirect behavior.
   - Use SHORT_DOMAIN from conftest. If domain variants are needed, they should be documented in the test or parameterized.

3. Create `tests/e2e/regression/test_tp34.py` (REG-04 -- Set redirect errors):
   - Module docstring with TP-34 bug description from Jira
   - Class `TestTP34` with `@pytest.mark.regression_bugs`
   - The "Set" feature likely involves link groups (is_set=1 in CreateMapRequest). Read the Jira ticket to understand the exact scenario.
   - If Sets require creating a link with is_set=1 via UI/API, do so inline as a precondition.
   - Test that redirecting through a Set link resolves correctly without errors.
   - If the Set feature cannot be tested (no API access to create sets), add `pytest.skip("Set creation not available in test environment")` with a comment explaining why.

All tests must:
- Use `@pytest.mark.regression_bugs` decorator on the class
- Use `http_client` fixture from conftest (not create their own httpx clients)
- Create their own preconditions inline (no shared test data)
- Use unique keywords with uuid to avoid caching interference
  </action>
  <verify>
Run: `cd tests/e2e && python -m pytest regression/test_tp25.py regression/test_tp29.py regression/test_tp34.py -m regression_bugs -v --timeout=30 2>&1 | head -40`
All tests should either pass or fail with clear assertion messages. No import errors, fixture errors, or unhandled exceptions.
  </verify>
  <done>Three test files exist, each with @pytest.mark.regression_bugs, descriptive docstrings referencing Jira tickets, and self-contained test methods that verify the specific redirect bug scenarios from TP-25, TP-29, and TP-34.</done>
</task>

</tasks>

<verification>
1. Run all redirect regression tests together: `cd tests/e2e && python -m pytest regression/ -m regression_bugs -v --timeout=60`
2. Verify no tests run in default mode: `cd tests/e2e && python -m pytest --co -q 2>&1 | grep -c regression` should be 0
3. Verify each test file has a module-level docstring with Jira ticket reference
4. Verify conftest fixtures are importable: `cd tests/e2e && python -c "from regression.conftest import *; print('OK')"`
</verification>

<success_criteria>
- 4 regression test files exist in tests/e2e/regression/ (test_tp22.py, test_tp25.py, test_tp29.py, test_tp34.py)
- conftest.py provides unique_keyword, http_client, api_client fixtures
- All tests are marked with @pytest.mark.regression_bugs
- Each test file documents the original bug behavior from the Jira ticket
- Running `pytest -m regression_bugs` executes these tests
- Running `pytest` (default) does NOT execute these tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-bug-regression-suite/16-01-SUMMARY.md`
</output>
