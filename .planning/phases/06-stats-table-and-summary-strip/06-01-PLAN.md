---
phase: 06-stats-table-and-summary-strip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/usage-dashboard-template.php
  - assets/css/usage-dashboard.css
autonomous: true

must_haves:
  truths:
    - "The usage dashboard template contains a sortable table structure with thead (4 sortable columns: date, hits, cost, balance) and an empty tbody for JS to populate"
    - "Summary card placeholders exist inside #tp-ud-summary-strip for JS rendering"
    - "Pagination container with info text and page list exists below the table"
    - "CSS provides gradient header styles, sortable header hover/active states, sort icon styling, column widths, and shimmer skeleton animation matching client-links design system"
    - "On mobile (max-width 767.98px), table converts to stacked card layout with data-label pseudo-elements"
    - "Summary cards display as horizontal strip on desktop and wrap on mobile"
    - "Empty state styling matches client-links pattern"
  artifacts:
    - path: "templates/usage-dashboard-template.php"
      provides: "Table HTML with sortable headers, tbody, pagination, summary strip, skeleton rows"
      contains: "tp-ud-sortable"
    - path: "assets/css/usage-dashboard.css"
      provides: "Table, summary card, pagination, skeleton, responsive card layout styles"
      contains: "tp-ud-sortable"
  key_links:
    - from: "templates/usage-dashboard-template.php"
      to: "assets/css/usage-dashboard.css"
      via: "CSS class names tp-ud-*"
      pattern: "tp-ud-table|tp-ud-sortable|tp-ud-pagination|tp-ud-stat-card"
---

<objective>
Build the HTML structure and CSS styles for the stats table, summary cards, pagination, and skeleton loading states.

Purpose: Provide the visual foundation that Phase 6 Plan 02 (JavaScript) will render data into. Without the template containers and CSS styles, the JS has nothing to target or style.

Output: Updated template with table structure, pagination, and summary card placeholders. Updated CSS with table gradient headers, sort icons, column widths, pagination, summary cards, shimmer skeletons, responsive mobile card layout, and estimated disclaimer styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-stats-table-and-summary-strip/06-RESEARCH.md
@.planning/phases/06-stats-table-and-summary-strip/06-CONTEXT.md

## Reference files (read for pattern replication, do NOT modify)
@templates/client-links-template.php  — table HTML structure with sortable headers, pagination container, skeleton tbody
@assets/css/client-links.css          — gradient headers, sort icons, pagination, shimmer skeleton, responsive card layout, CSS variables
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add table HTML structure, summary strip placeholders, and pagination to the template</name>
  <files>templates/usage-dashboard-template.php</files>
  <action>
Modify the existing template to replace the empty placeholder divs with proper HTML structure. The changes go inside `#tp-ud-content`, between the existing date header and chart sections.

**Summary strip** (`#tp-ud-summary-strip`): Keep the existing empty div. JS will render cards into it. No static HTML needed -- the skeleton is shown via the existing `#tp-ud-skeleton` section. However, add a summary-specific skeleton section inside `#tp-ud-skeleton` that shows 3 shimmer card placeholders (replacing the current plain `tp-ud-skeleton-stats` blocks with shimmer-animated versions matching client-links skeleton pattern).

**Table** (`#tp-ud-table-container`): Replace the empty div with a full table structure:
```html
<div id="tp-ud-table-container" style="display: none;">
    <div class="table-responsive">
        <table class="table table-hover tp-ud-table" id="tp-ud-table">
            <thead>
                <tr>
                    <th class="tp-ud-col-date tp-ud-sortable" data-sort="date">
                        Date <i class="fas fa-sort tp-ud-sort-icon"></i>
                    </th>
                    <th class="tp-ud-col-hits tp-ud-sortable" data-sort="totalHits">
                        Hits (est.) <i class="fas fa-sort tp-ud-sort-icon"></i>
                    </th>
                    <th class="tp-ud-col-cost tp-ud-sortable" data-sort="hitCost">
                        Cost <i class="fas fa-sort tp-ud-sort-icon"></i>
                    </th>
                    <th class="tp-ud-col-balance tp-ud-sortable" data-sort="balance">
                        Balance <i class="fas fa-sort tp-ud-sort-icon"></i>
                    </th>
                </tr>
            </thead>
            <tbody id="tp-ud-tbody"></tbody>
        </table>
    </div>

    <!-- Estimated disclaimer footnote -->
    <p class="tp-ud-estimated-note">
        <i class="fas fa-info-circle"></i>
        Click/QR breakdown is estimated from total hits.
    </p>

    <!-- Pagination (same structure as client-links) -->
    <div class="tp-ud-pagination" id="tp-ud-pagination" style="display: none;">
        <div class="tp-ud-pagination-info">
            <span id="tp-ud-pagination-info">Showing 0-0 of 0</span>
        </div>
        <nav aria-label="Usage stats pagination">
            <ul class="pagination pagination-sm mb-0" id="tp-ud-pagination-list"></ul>
        </nav>
    </div>
</div>
```

**Empty state**: Add an empty state div (hidden by default) inside `#tp-ud-content`, after the table container:
```html
<div id="tp-ud-empty" class="tp-ud-empty" style="display: none;">
    <i class="fas fa-chart-bar fa-3x text-muted mb-3 d-block"></i>
    <h5>No usage data</h5>
    <p class="text-muted" id="tp-ud-empty-range"></p>
</div>
```

**Important:** The table container starts hidden (`display: none`). JS will show it after data loads. Move the existing `tp-ud-no-data` logic to use the new empty state div instead.

Update the skeleton section: Replace the current `tp-ud-skeleton-stats` block divs with shimmer-animated versions that have the `tp-ud-skel` class for the shimmer effect, matching the column layout of the real table. Add skeleton table rows inside the skeleton section.

Table header text should use `esc_html_e()` for i18n consistency with the rest of the template.
  </action>
  <verify>
Verify the template by reading the file and confirming:
1. `#tp-ud-table` exists with `<thead>` containing 4 `th.tp-ud-sortable` elements with correct `data-sort` values (date, totalHits, hitCost, balance)
2. `#tp-ud-tbody` is an empty `<tbody>` for JS population
3. `#tp-ud-pagination` div exists with `#tp-ud-pagination-info` and `#tp-ud-pagination-list`
4. `#tp-ud-empty` div exists with `#tp-ud-empty-range` paragraph
5. `.tp-ud-estimated-note` paragraph exists with disclaimer text
6. Skeleton section has shimmer-animated table skeleton rows
  </verify>
  <done>
Template contains the full table HTML structure (sortable headers with data-sort attributes, empty tbody, pagination container, empty state div, estimated disclaimer, and shimmer skeleton rows) ready for JS rendering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add table, summary card, pagination, skeleton, and responsive styles to CSS</name>
  <files>assets/css/usage-dashboard.css</files>
  <action>
Extend the existing CSS file with new sections. The CSS uses the same design system variables as client-links (the `:root` variables are already defined in client-links.css which loads on the same page). All class names use the `tp-ud-` prefix.

Add these CSS sections in order:

**1. Shimmer keyframes** (replace the existing `tp-ud-pulse` or add alongside it):
```css
@keyframes tp-ud-shimmer {
    0% { background-position: -400px 0; }
    100% { background-position: 400px 0; }
}
```

**2. Summary cards strip**:
- `.tp-ud-summary-strip`: `display: flex; gap: 16px; margin-bottom: 20px;`
- `.tp-ud-stat-card`: Card with border, border-radius 0.75rem, padding, flex row layout (icon + body), `background: var(--tp-surface)`, `border: 1px solid var(--tp-border)`, subtle box-shadow
- `.tp-ud-stat-icon`: 40px circle with `background: var(--tp-section)`, centered icon, `color: var(--tp-primary)`
- `.tp-ud-stat-body`: flex column
- `.tp-ud-stat-value`: `font-size: 1.25rem; font-weight: 700; color: var(--tp-text)`
- `.tp-ud-stat-label`: `font-size: .75rem; text-transform: uppercase; letter-spacing: .05em; color: var(--tp-muted); font-weight: 600`
- `.tp-ud-stat-secondary`: `font-size: .8rem; color: var(--tp-muted)`
- Summary card skeletons: `.tp-ud-stat-card-skeleton` with shimmer animated placeholder blocks

**3. Table styles** (match client-links gradient headers pattern):
- `.tp-ud-table`: `margin-bottom: 0`
- `.tp-ud-table thead th`: `background: linear-gradient(180deg, #eef4ff, #e4edff); border-bottom: 2px solid var(--tp-border); font-weight: 700; font-size: .78rem; text-transform: uppercase; letter-spacing: .06em; color: var(--tp-primary-contrast); white-space: nowrap; padding: .85rem 1rem; user-select: none;`
- `.tp-ud-table tbody td`: `vertical-align: middle; padding: .85rem 1rem; border-bottom: 1px solid rgba(207,226,255,.5); background: var(--tp-surface);`
- `.tp-ud-table tbody tr:hover td`: `background: var(--tp-surface-soft);`

**4. Column widths**:
- `.tp-ud-col-date`: `width: 25%; min-width: 110px`
- `.tp-ud-col-hits`: `width: 30%; min-width: 130px`
- `.tp-ud-col-cost`: `width: 20%; min-width: 90px`
- `.tp-ud-col-balance`: `width: 25%; min-width: 100px`

**5. Sortable headers** (same pattern as client-links):
- `.tp-ud-sortable`: `cursor: pointer; transition: color .15s;`
- `.tp-ud-sortable:hover`: `color: var(--tp-primary);`
- `.tp-ud-sort-icon`: `font-size: .7rem; margin-left: 4px; opacity: .4; transition: opacity .15s;`
- `.tp-ud-sort-active .tp-ud-sort-icon`: `opacity: 1; color: var(--tp-primary);`

**6. Hits cell** (breakdown with icons):
- `.tp-ud-hits-cell`: flex column layout
- `.tp-ud-hits-total`: `font-weight: 600`
- `.tp-ud-hits-breakdown`: `font-size: .8rem; color: var(--tp-muted)` with icon spacing

**7. Cost and balance cells**:
- `.tp-ud-cost, .tp-ud-balance`: `font-family: SFMono-Regular, Menlo, Monaco, Consolas, monospace; font-size: .875rem;`

**8. Estimated disclaimer**:
- `.tp-ud-estimated-note`: `font-size: .78rem; color: var(--tp-muted); padding: .5rem 1rem; margin: 0;` with info-circle icon styling

**9. Pagination** (replicate client-links pagination exactly):
- `.tp-ud-pagination`: `display: flex; justify-content: space-between; align-items: center; padding: .85rem 1.25rem; background: linear-gradient(180deg, #f0f6ff, #e8f0ff); border-top: 1px solid var(--tp-border);`
- `.tp-ud-pagination-info`: `font-size: .85rem; color: var(--tp-muted); font-weight: 500;`
- `.tp-ud-pagination .page-link`: same styles as `.tp-cl-pagination .page-link`
- `.tp-ud-pagination .page-item.active .page-link`: gradient active state
- `.tp-ud-pagination .page-item.disabled .page-link`: disabled state

**10. Empty state**:
- `.tp-ud-empty`: `text-align: center; padding: 3rem 1.5rem; background: var(--tp-section); border-radius: 1rem; border: 1px solid rgba(60,122,229,.2);`
- `.tp-ud-empty i`: `color: var(--tp-muted);`
- `.tp-ud-empty h5`: `color: var(--tp-primary-contrast); font-weight: 700;`

**11. Skeleton table rows** (shimmer animation):
- `.tp-ud-skel`: `height: 16px; background: linear-gradient(90deg, #eef4ff 25%, #dce9ff 50%, #eef4ff 75%); background-size: 400px 100%; animation: tp-ud-shimmer 1.5s infinite linear; border-radius: .375rem;`
- Size variants: `.tp-ud-skel-lg` (20px/85%), `.tp-ud-skel-md` (16px/70%), `.tp-ud-skel-sm` (14px/60%)
- Staggered animation delays for skeleton rows (nth-child 1-10)

**12. Responsive -- mobile card layout** (`@media (max-width: 767.98px)`):
- Table becomes block layout, thead hidden
- Each row becomes a card with border, border-radius, padding, box-shadow
- Each td becomes flex row with `::before` pseudo-element showing `data-label` attribute
- Column width overrides removed (width: auto)
- Summary strip wraps: `flex-wrap: wrap;` with cards taking `flex: 1 1 calc(50% - 8px); min-width: 140px;`
- Pagination stacks vertically
- Touch-friendly pagination links (min 44px targets)

Do NOT modify or duplicate the existing `:root` CSS variables -- they are already defined in client-links.css which is loaded on the same WordPress site.
  </action>
  <verify>
Read the CSS file and confirm:
1. `@keyframes tp-ud-shimmer` exists
2. `.tp-ud-stat-card` styles exist with flex layout
3. `.tp-ud-table thead th` has gradient background and uppercase text
4. `.tp-ud-sortable` cursor pointer and hover state exist
5. `.tp-ud-sort-icon` and `.tp-ud-sort-active .tp-ud-sort-icon` exist
6. Column width classes defined for all 4 columns
7. `.tp-ud-pagination` with flex layout and gradient background exists
8. `@media (max-width: 767.98px)` section converts table to card layout with `::before` data-label
9. `.tp-ud-empty` styles match client-links empty pattern
10. `.tp-ud-skel` shimmer styles exist
  </verify>
  <done>
CSS file contains complete styles for the usage stats table (gradient headers, sortable icons, column widths), summary cards (horizontal strip with icon + value + label), pagination (gradient footer, active/disabled states), shimmer skeleton animation, empty state, estimated disclaimer, and responsive mobile card layout (stacked cards with data-label pseudo-elements) -- all matching the client-links design system.
  </done>
</task>

</tasks>

<verification>
1. Read `templates/usage-dashboard-template.php` and verify the table structure with sortable headers, pagination, empty state, and skeleton
2. Read `assets/css/usage-dashboard.css` and verify all style sections exist
3. Confirm no modifications to any files outside the `files_modified` list
4. Confirm the CSS does NOT duplicate `:root` variable declarations
</verification>

<success_criteria>
- Template has sortable table headers with data-sort attributes matching the 4 columns (date, totalHits, hitCost, balance)
- Pagination HTML structure matches client-links pattern
- CSS gradient headers, sort icons, and pagination styles visually match client-links
- Mobile responsive breakpoint converts table to stacked cards with data-label
- Shimmer skeleton animation replaces or supplements the existing pulse animation
- Empty state styled consistently with client-links
- Estimated disclaimer appears below the table
</success_criteria>

<output>
After completion, create `.planning/phases/06-stats-table-and-summary-strip/06-01-SUMMARY.md`
</output>
