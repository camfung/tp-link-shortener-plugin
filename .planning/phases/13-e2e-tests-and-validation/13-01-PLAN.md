---
phase: 13-e2e-tests-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_usage_dashboard_table.py
  - tests/e2e/test_usage_dashboard_other_services.py
autonomous: false
requirements:
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "PHPUnit merge adapter tests pass (9 tests, all scenarios covered)"
    - "Integration test confirms wallet client fetches real data for uid 125"
    - "E2E test verifies Other Services column header appears in the table"
    - "E2E test verifies Other Services amounts display in +$X.XX format"
    - "E2E test verifies Bootstrap tooltip appears on hover over Other Services amount"
    - "E2E test verifies Other Services summary card (4th card) is visible"
    - "Existing E2E tests still pass after updating assertions for 5-column table"
  artifacts:
    - path: "tests/e2e/test_usage_dashboard_other_services.py"
      provides: "E2E tests for Other Services column, tooltips, and summary card"
      min_lines: 80
    - path: "tests/e2e/test_usage_dashboard_table.py"
      provides: "Updated existing E2E tests with 5-column and 4-card assertions"
  key_links:
    - from: "tests/e2e/test_usage_dashboard_other_services.py"
      to: "templates/usage-dashboard-template.php"
      via: "Playwright selectors targeting .tp-ud-col-other, .tp-ud-other-amount, .tooltip-inner"
      pattern: "tp-ud-other|tp-ud-col-other|tooltip-inner"
    - from: "tests/e2e/test_usage_dashboard_table.py"
      to: "templates/usage-dashboard-template.php"
      via: "Updated column count assertions (4->5) and card count (3->4)"
      pattern: "== 5|== 4"
---

<objective>
Finalize v2.2 TerrWallet integration testing: update existing E2E tests for the 5-column table layout, create new E2E tests for the Other Services column with tooltip verification, run PHPUnit tests locally, and verify the integration test on the server.

Purpose: Complete the test pyramid for the TerrWallet feature -- unit tests (merge adapter), integration tests (wallet client), and E2E tests (rendered UI) all green.
Output: Updated test_usage_dashboard_table.py, new test_usage_dashboard_other_services.py, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-e2e-tests-and-validation/13-RESEARCH.md
@tests/e2e/conftest.py
@tests/e2e/test_usage_dashboard_table.py
@tests/Unit/TerrWallet/UsageMergeAdapterTest.php
@tests/Integration/test-terrwallet-client.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing E2E tests for 5-column table and 4 summary cards</name>
  <files>tests/e2e/test_usage_dashboard_table.py</files>
  <action>
Phase 12 added the Other Services column (5th sortable header) and 4th summary card. Update the existing E2E test file to match the new table structure:

1. `test_table_has_four_column_headers` (line 74-79): Rename to `test_table_has_five_column_headers`. Change assertion from `== 4` to `== 5`. Update error message.

2. `test_column_header_data_sort_values` (line 81-87): Update the expected list to match the actual column order in the template (usage-dashboard-template.php lines 126-138). The correct order is: `["date", "totalHits", "otherServices", "hitCost", "balance"]`. Other Services is the 3rd column, between Hits and Cost.

3. `test_table_rows_have_data` (line 89-101): Update cell count assertion from `== 4` to `== 5`. Update error message.

4. `test_three_summary_cards` (line 298-306): Rename to `test_four_summary_cards`. Change assertion from `== 3` to `== 4`. Update error message.

5. `test_sort_header_click_adds_active` (line 169): Add `"otherServices"` to the parametrize list so it becomes `["date", "totalHits", "hitCost", "balance", "otherServices"]`.

Do NOT change any other tests. The logic for date, hits, cost, balance tests remains identical.
  </action>
  <verify>Run `cd tests/e2e && python -m pytest test_usage_dashboard_table.py --co -q` to confirm test collection (no syntax errors). Note: full E2E execution requires deploying via `deploy.sh feature/client-links` and running `pytest tests/e2e/ -v` against the live dev site -- collection-only is the local verification step.</verify>
  <done>All 5 assertions updated (header count 5, sort values include otherServices in correct order, cell count 5, card count 4, sort parametrize includes otherServices). Test file collects without errors. Full execution deferred to post-deployment checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E tests for Other Services column and tooltips</name>
  <files>tests/e2e/test_usage_dashboard_other_services.py</files>
  <action>
Create a new Playwright E2E test file following the exact patterns from test_usage_dashboard_table.py. Use the same helper functions (_check_deployment, _require_deployment, _wait_for_data, _has_table_data) duplicated at the top (same pattern as the existing file -- each E2E test file is self-contained with its own helpers).

Import from conftest: `BASE_URL, USAGE_DASHBOARD_PATH`

Create these test classes:

**Class TestOtherServicesColumn:**

1. `test_other_services_header_visible` -- After _wait_for_data, assert `th.tp-ud-sortable[data-sort="otherServices"]` is visible.

2. `test_other_services_header_is_sortable` -- Click the Other Services header, assert it gets `tp-ud-sort-active` class and sort icon changes to `fa-sort-up` or `fa-sort-down`.

3. `test_other_services_cell_format` -- Find the first row's `.tp-ud-col-other` cell. It should either contain `.tp-ud-other-amount` (with +$X.XX format text) or `.tp-ud-other-zero` (showing $0.00) or a dash. Assert one of these states.

4. `test_other_services_amounts_have_plus_prefix` -- Use `page.locator('.tp-ud-other-amount')`. If count > 0, verify `.inner_text()` starts with `+$` and matches pattern `\+\$\d+\.\d{2}$`.

**Class TestOtherServicesTooltip:**

5. `test_tooltip_appears_on_hover` -- Find `.tp-ud-other-amount` elements. If none exist, `pytest.skip("No wallet data in current date range")`. Hover over `amounts.first`. Wait for `.tooltip-inner` to appear (timeout 3000ms) -- IMPORTANT: Bootstrap tooltip renders to body with `container: 'body'`, so use `page.locator('.tooltip-inner')` NOT inside the cell. Assert tooltip text length > 0.

6. `test_tooltip_disappears_after_unhover` -- Hover over amount, wait for tooltip visible, then hover over a different element (e.g., the table header). Assert `.tooltip-inner` becomes hidden or detached (count == 0 or not visible).

**Class TestOtherServicesSummaryCard:**

7. `test_fourth_summary_card_visible` -- Assert `.tp-ud-stat-card:nth-child(4)` is visible.

8. `test_fourth_card_has_icon` -- Assert `.tp-ud-stat-card:nth-child(4) .fa-hand-holding-dollar` exists (count > 0).

9. `test_fourth_card_has_dollar_value` -- Get the 4th card's `.tp-ud-stat-value` text. Assert it contains `$` and matches `\+?\$\d+\.\d{2}$` pattern.

All tests should skip gracefully with `pytest.skip()` when no data is available (same pattern as existing tests).

File docstring should note: "Requires Phase 12 code deployed to dev site."
  </action>
  <verify>Run `cd tests/e2e && python -m pytest test_usage_dashboard_other_services.py --co -q` to confirm 9 tests collected (no syntax errors). Note: full E2E execution requires deploying via `deploy.sh feature/client-links` and running `pytest tests/e2e/ -v` against the live dev site -- collection-only is the local verification step.</verify>
  <done>New test file with 9 tests covering Other Services column visibility, sorting, cell format, amount format, tooltip hover/unhover, and summary card (visibility, icon, value). All tests collect without syntax errors. Full execution deferred to post-deployment checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Run PHPUnit merge adapter tests locally (TEST-02)</name>
  <files></files>
  <action>
Run the PHPUnit merge adapter test suite to confirm all 9 tests pass with all assertions green. This is the local verification for TEST-02.

Execute: `cd /Users/camer/Documents/projects/tp-bloomland/homepage/tp-link-shortener-plugin && vendor/bin/phpunit tests/Unit/TerrWallet/UsageMergeAdapterTest.php`

Expected output: "OK (9 tests, 49 assertions)" or similar. If any test fails, diagnose and report -- do NOT modify test code (the tests are the source of truth for merge adapter behavior).
  </action>
  <verify>PHPUnit output shows "OK" with 9 tests passing and 49+ assertions.</verify>
  <done>All 9 PHPUnit merge adapter tests pass locally. TEST-02 confirmed green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run integration test on server (TEST-01)</name>
  <action>
TEST-01 requires running the wallet client integration test on the WordPress server because it depends on the full WordPress/WooCommerce environment (TerrWallet plugin, database, user data for uid 125). This cannot be run locally or by Claude.

Steps:
1. SSH into the server (or use WP-CLI remotely)
2. Navigate to the plugin directory
3. Run: `wp eval-file tests/Integration/test-terrwallet-client.php`
4. Expected output: "SUCCESS: Retrieved N credit transaction(s)" where N > 0

If the test fails:
- "Call to undefined function" -- TerrWallet plugin may not be active
- "Retrieved 0 transactions" -- uid 125 may not have credit transactions in the date range (check 2026-01-01 to current date)
- Fatal error -- check that the plugin code is deployed (`deploy.sh feature/client-links`)
  </action>
  <resume-signal>Report the output of `wp eval-file tests/Integration/test-terrwallet-client.php`. Type "pass" if it shows SUCCESS, or paste the error output.</resume-signal>
</task>

</tasks>

<verification>
1. `cd /Users/camer/Documents/projects/tp-bloomland/homepage/tp-link-shortener-plugin && vendor/bin/phpunit tests/Unit/TerrWallet/UsageMergeAdapterTest.php` -- All 9 tests pass (TEST-02)
2. `cd tests/e2e && python -m pytest test_usage_dashboard_table.py --co -q` -- Updated tests collect without errors
3. `cd tests/e2e && python -m pytest test_usage_dashboard_other_services.py --co -q` -- 9 new tests collect without errors
4. User confirms `wp eval-file tests/Integration/test-terrwallet-client.php` returns SUCCESS on server (TEST-01)
5. Note: Full E2E execution requires deployment via `deploy.sh feature/client-links` and running `pytest tests/e2e/ -v` on a machine with the .env configured.
</verification>

<success_criteria>
- PHPUnit merge adapter tests pass locally (9 tests, 49+ assertions) -- TEST-02 confirmed by execution, not just collection
- Integration test passes on server via wp eval-file -- TEST-01 confirmed by user at checkpoint
- Existing E2E tests updated for 5-column layout (no regressions)
- New E2E test file covers Other Services column, tooltips, and summary card -- TEST-03
- All E2E test files collect without syntax errors locally; full execution requires deployment
</success_criteria>

<output>
After completion, create `.planning/phases/13-e2e-tests-and-validation/13-01-SUMMARY.md`
</output>
